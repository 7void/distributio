const https = require('https'); https.get('https://html.duckduckgo.com/html/?q=beyond+snack+shark+tank+maharashtra+expansion', (res) => { let data = ''; res.on('data', (c) => data += c); res.on('end', () => { const matches = data.match(/<a class="result__snippet[^>]*>(.*?)<\/a>/gi); if(matches) { matches.slice(0, 5).forEach(m => console.log(m.replace(/<[^>]+>/g, ' ').trim())); } }); });
