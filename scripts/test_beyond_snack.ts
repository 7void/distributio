import { scoreCities } from "../lib/score"; import type { ExtractedFeatures, ProductProfile } from "../lib/types"; import cities from "../data/cities.json"; const features = { productName: "Beyond Snack Kerala Banana Chips", category: "Snacks", priceINR: 85, priceSegment: "premium", incomeWeight: 0.30, retailWeight: 0.25, internetWeight: 0.25, coldWeight: 0.0, logisticsWeight: 0.20, affordability: 0.85, targetAudience: "Urban millennials", needsColdChain: false, distributionLevel: 1, distributionType: "selective", distributorProfile: "retailer", channels: ["Quick Commerce", "Modern Trade", "D2C"], keyInsight: "Premium regional snack.", seasonality: "Neutral" }; const profile = { productName: "Beyond Snack Kerala Banana Chips", priceINR: 85, marginPercent: 35, brandMaturity: "emerging", warehouseCity: "Mumbai", deliveryRadiusKM: 1500, incomeTarget: "premium", preferredChannels: ["Quick Commerce", "Modern Trade"], hasDistributor: "yes", primaryGoal: "revenue", launchBudgetINR: "?5L – ?20L" }; const scored = scoreCities(features as any, profile as any, undefined, cities as any); console.log("=== BEYOND SNACK: DISTRIBUTIO TOP 10 CITIES ==="); scored.slice(0, 10).forEach((city, index) => { console.log(`${(index + 1).toString().padStart(2, " ")}. ${city.name.padEnd(15)} | Band: ${city.band.padEnd(10)} | Score: ${city.score} | Logistics Cost: Rs ${city.logisticsCostPerUnit}`); }); console.log("\n=== BOTTOM 5 CITIES (Sanity Check) ==="); scored.slice(-5).forEach((city, index) => { console.log(`${(scored.length - 4 + index).toString().padStart(2, " ")}. ${city.name.padEnd(15)} | Band: ${city.band.padEnd(10)} | Score: ${city.score}`); });
