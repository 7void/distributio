import { scoreCities } from "../lib/score"; import type { ExtractedFeatures, ProductProfile } from "../lib/types"; import cities from "../data/cities.json"; const features = { productName: "Starbucks Premium Coffee", category: "Beverages", priceINR: 350, priceSegment: "luxury", incomeWeight: 0.40, retailWeight: 0.25, internetWeight: 0.10, coldWeight: 0.15, logisticsWeight: 0.10, affordability: 0.55, targetAudience: "High-income urban professionals", needsColdChain: true, distributionLevel: 0, distributionType: "exclusive", distributorProfile: "direct", channels: ["Specialty Retail"], keyInsight: "Luxury cafe model.", seasonality: "Neutral" }; const profile = { productName: "Starbucks Premium Coffee", priceINR: 350, marginPercent: 60, brandMaturity: "established", warehouseCity: "Mumbai", deliveryRadiusKM: 2000, incomeTarget: "premium", preferredChannels: ["Specialty Retail"], hasDistributor: "direct", primaryGoal: "brand_awareness", launchBudgetINR: "Above ?1Cr" }; const scored = scoreCities(features as any, profile as any, undefined, cities as any); console.log("=== STARBUCKS: DISTRIBUTIO TOP 15 CITIES ==="); scored.slice(0, 15).forEach((city, index) => { console.log(`${(index + 1).toString().padStart(2, " ")}. ${city.name.padEnd(15)} | Band: ${city.band.padEnd(10)} | Tier: ${city.tier} | Score: ${city.score}`); }); console.log("\n=== BOTTOM 5 CITIES ==="); scored.slice(-5).forEach((city, index) => { console.log(`${(scored.length - 4 + index).toString().padStart(2, " ")}. ${city.name.padEnd(15)} | Band: ${city.band.padEnd(10)} | Tier: ${city.tier} | Score: ${city.score}`); });
